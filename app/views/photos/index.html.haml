%h2= "Photos for #{@student.name}"
= link_to "Back to profile", student_path(@student)

%ul.photos
  - @student.photos.each do |photo|
    %li.photo
      = link_to image_tag(photo.image.url('250x200')), student_photo_path(@student, photo)
      %p.comment
        = photo.comment

%h3 Add new photo
- form_for [@student, Photo.new], :html => {:multipart => true} do |f|
  %div
    = f.label :image
    = f.file_field :image
  %div
    = f.label :comment
    = f.text_area :comment
  %div
    = f.submit "Add"
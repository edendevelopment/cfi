%h1 People

%h2 Add new person
- form_for Person.new do |f|
  %p.field
    = f.label :name
    = f.text_field :name
    
  %p.field
    %label
      = f.radio_button :gender, 'Female', :checked => true
      Female
    %label
      = f.radio_button :gender, 'Male'
      Male

  = f.submit "Add person"

%h2 List of people

%ul.people
  - Person.all.each do |person|
    %li= render :partial => "people/person", :locals => {:person => person}

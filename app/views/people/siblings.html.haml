%h1 
  Edit siblings for
  = @person.name
  
- form_for @person, :html => { :multipart => true } do |form|
  %p.field
    = form.label :siblings
    %br
    = form.text_area :siblings
  
  = form.submit "Save"
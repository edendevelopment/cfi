%h2 Siblings
%ul.people
  - @person.siblings.each do |sibling|
    %li= render :partial => "people/person", :locals => {:person => sibling}
      
= link_to "Edit siblings", siblings_person_path(@person)